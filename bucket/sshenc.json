{
    "version": "0.6.87",
    "description": "Hardware-backed SSH key management (Windows TPM 2.0)",
    "homepage": "https://github.com/godaddy/sshenc",
    "license": "MIT",
    "architecture": {
        "64bit": {
            "url": "https://github.com/godaddy/sshenc/releases/download/v0.6.87/sshenc-x86_64-pc-windows-msvc.zip",
            "hash": "7c3282a94a1ef2e58e5ed5524f6345de57701575df47edb8d17aaba5d06889a1"
        }
    },
    "bin": [
        "sshenc.exe",
        "sshenc-keygen.exe",
        "sshenc-agent.exe",
        "gitenc.exe",
        "sshenc-tpm-bridge.exe"
    ],
    "post_install": [
        "$pd = Join-Path $env:ProgramData \"sshenc\"",
        "if (-not (Test-Path $pd)) { New-Item -ItemType Directory -Path $pd -Force | Out-Null }",
        "try { Copy-Item (Join-Path $dir \"sshenc-tpm-bridge.exe\") -Destination (Join-Path $pd \"sshenc-tpm-bridge.exe\") -Force -ErrorAction Stop; Write-Host \"sshenc: bridge synced to $pd\" } catch { Write-Warning \"sshenc: failed to copy bridge to $pd ($_); WSL callers may pick up a stale bridge. If in use, stop sshenc-agent first (Stop-Process -Name sshenc-agent on Windows, pkill sshenc-agent in WSL). If permission denied, re-run scoop from an elevated shell, or set SSHENC_BRIDGE_PATH to the scoop path.\" }"
    ]
}
